# Производительность генераторов

## Сравнение скорости (на M4 Mac)

### На 1 ядро

| Сеть | Библиотека | Скорость | Примечание |
|------|-----------|----------|------------|
| **Solana** | solders | ~15 000 addr/s | Ed25519, Rust |
| **EVM** | coincurve | ~41 000 addr/s | secp256k1, C |
| **EVM** | eth-account | ~700 addr/s | ⚠️ медленный fallback |
| **Tron** | coincurve+base58 | ~40 000 addr/s | secp256k1 (как ETH) |
| **TON** | mock | ~50 000 addr/s | заглушка |

### На 9 ядрах (M4)

| Сеть | Скорость |
|------|----------|
| **Solana** | ~135 000 addr/s |
| **EVM (coincurve)** | ~370 000 addr/s |
| **Tron** | ~360 000 addr/s |
| **TON (mock)** | ~450 000 addr/s |

*С учётом overhead multiprocessing и проверки условий*

## Оптимизация EVM/Tron

### До оптимизации (eth-account)
- ~700 addr/s на ядро
- Много слоёв абстракции, медленная криптография

### После оптимизации (coincurve + pycryptodome)
- ~41 000 addr/s на ядро
- Прямая работа с C библиотеками (libsecp256k1)
- Минимум Python overhead

**Ускорение: ~59x**

## Бутылочное горлышко

На 1000 адресов ETH:
| Этап | Время | % |
|------|-------|---|
| Генерация ключа | 0.9ms | 4% |
| secp256k1 (pubkey) | 17.4ms | 74% |
| Keccak-256 | 5.2ms | 22% |

## Рекомендации

1. **EVM** — самый быстрый для CPU генерации
2. **Tron** — почти не уступает ETH (Base58 даёт ~2% overhead)
3. Запускайте одну сеть для максимальной скорости всех ядер
4. Для редких паттернов запускайте ETH на ночь
